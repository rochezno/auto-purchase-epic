{
  "dependencies": {
    "puppeteer": "^15.4.0"
  },
  "devDependencies": {
    "gts": "^3.1.0",
    "typescript": "^4.7.4",
    "@types/node": "^14.11.2"
  },
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "npx tsc",
    "start": "node app.ts",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint"
  }
}
